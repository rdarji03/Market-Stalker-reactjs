import React from 'react';
import { useQuery } from 'react-query';

const Home = () => {
    return (
        <div className='stock-container' > 
            
        </div>
    );
}

export default Home;
